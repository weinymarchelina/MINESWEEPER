#ifndef MAIN_H
#define MAIN_H

#include <iostream>
#include <string>
#include <vector>
#include <fstream>
#include <sstream>
#include <cctype>

using namespace std;

enum State {
    startState = 0,
    standByState,
    playingState,
    gameOverState
};

#endif // MAIN_H
